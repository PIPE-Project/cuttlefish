---
# Variables for this role come from group_vars/secrets.yml (gitignored).
# Copy provisioning/group_vars/secrets.yml.example to secrets.yml and fill in your values.
#
# Required variables:
#   secret_key_base, devise_secret_key, jwt_secret, cuttlefish_hash_salt,
#   db_password, certbot_email, github_users
#
# See secrets.yml.example for the full list including optional variables.
