# Install required Galaxy roles with:
#   cd provisioning
#   ansible-galaxy install -r requirements.yml -p roles
#
# Or run ./provision_production.sh which handles this automatically.

- src: rvm.ruby
  version: v2.4.0

- src: geerlingguy.certbot
  version: 7.1.2

- src: DavidWittman.redis
  version: 1.2.8

- src: nickhammond.logrotate
  version: v0.0.5

# Optional: install only if new_relic_enabled: true in group_vars/all
- src: newrelic.newrelic-infra
  version: 0.8.2
